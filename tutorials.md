Backend Development with Node.js
First what I need is: packege.json file as we are working with node.js. To get it, the command: npm init -y
The entry point of our folder/files we need which is created for now as server.js

We are now going to install express.js, a powerfull library/framwork to buildings API in node.js.
command: npm install express. We aslo need nodemon. Command: npm install nodemon --save-dev